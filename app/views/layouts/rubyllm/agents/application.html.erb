<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>RubyLLM Agents Dashboard</title>

  <!-- Tailwind CSS via CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Turbo -->
  <%= javascript_include_tag "turbo", type: "module" rescue nil %>

  <style>
    /* Custom styles for the dashboard */
    .stat-card {
      @apply bg-white rounded-lg shadow p-6;
    }
    .stat-value {
      @apply text-3xl font-bold text-gray-900;
    }
    .stat-label {
      @apply text-sm text-gray-500 uppercase tracking-wide;
    }
    .nav-link {
      @apply px-4 py-2 text-gray-600 hover:text-gray-900 hover:bg-gray-100 rounded-md transition-colors;
    }
    .nav-link.active {
      @apply bg-blue-50 text-blue-700;
    }
    .badge {
      @apply inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium;
    }
    .badge-success {
      @apply bg-green-100 text-green-800;
    }
    .badge-error {
      @apply bg-red-100 text-red-800;
    }
    .badge-timeout {
      @apply bg-yellow-100 text-yellow-800;
    }
  </style>
</head>
<body class="bg-gray-50 min-h-screen">
  <!-- Header -->
  <header class="bg-white shadow-sm border-b">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <div class="flex items-center space-x-4">
          <h1 class="text-xl font-semibold text-gray-900">
            🤖 RubyLLM Agents
          </h1>
          <nav class="flex space-x-2">
            <%= link_to "Dashboard", ruby_llm_agents.root_path, class: "nav-link #{current_page?(ruby_llm_agents.root_path) ? 'active' : ''}" %>
            <%= link_to "Executions", ruby_llm_agents.executions_path, class: "nav-link #{current_page?(ruby_llm_agents.executions_path) ? 'active' : ''}" %>
          </nav>
        </div>
        <div class="text-sm text-gray-500">
          <%= Time.current.strftime("%B %d, %Y") %>
        </div>
      </div>
    </div>
  </header>

  <!-- Main content -->
  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <%= yield %>
  </main>

  <!-- Footer -->
  <footer class="border-t bg-white mt-auto">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
      <p class="text-center text-sm text-gray-500">
        Powered by <a href="https://github.com/adham90/ruby_llm-agents" class="text-blue-600 hover:underline">ruby_llm-agents</a>
      </p>
    </div>
  </footer>
</body>
</html>
